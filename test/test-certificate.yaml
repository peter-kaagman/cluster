apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-prjv
  namespace: cert-manager

spec:
  secretName: wildcard-prjv-tls

  issuerRef:
    name: letsencrypt-dns01
    kind: ClusterIssuer

  dnsNames:
    - "*.prjv.nl"
    - prjv.nl